# 📱 CROPIC - Quick Command Reference

## 🚀 Quick Start (3 Steps)

```bash
# 1. Install dependencies
cd /workspaces/pmfby-app
flutter pub get

# 2. Setup Firebase (one-time)
flutterfire configure --project=cropic

# 3. Run app
flutter run
```

## 📋 Essential Commands

### Development
```bash
# Run app (debug mode)
flutter run

# Run with hot reload
flutter run --hot

# Clean build
flutter clean && flutter pub get && flutter run

# Check for issues
flutter doctor
flutter analyze
```

### Build Release
```bash
# Build APK
flutter build apk --release

# Build App Bundle (Play Store)
flutter build appbundle --release

# Install APK on device
flutter install --release
```

### Testing
```bash
# Run all tests
flutter test

# Run specific test
flutter test test/widget_test.dart

# Test coverage
flutter test --coverage
```

## 🔧 Troubleshooting Commands

```bash
# Fix pub get issues
flutter pub cache repair
flutter pub get

# Fix gradle issues
cd android && ./gradlew clean
cd .. && flutter clean

# List devices
flutter devices

# View logs
flutter logs

# Check app size
flutter build apk --analyze-size
```

## 📦 Firebase Commands

```bash
# Install FlutterFire CLI
dart pub global activate flutterfire_cli

# Configure Firebase
flutterfire configure

# Update Firebase
flutterfire reconfigure
```

## 🎨 Code Generation (Future)

```bash
# Generate code (if using build_runner)
flutter pub run build_runner build

# Watch for changes
flutter pub run build_runner watch
```

## 📱 Device Commands

```bash
# List connected devices
adb devices

# Install APK manually
adb install build/app/outputs/flutter-apk/app-release.apk

# Check app logs
adb logcat | grep flutter

# Screenshot
adb shell screencap -p /sdcard/screen.png
adb pull /sdcard/screen.png
```

## 🗂️ Project Structure Quick Reference

```
lib/
├── main.dart                     # Entry point, routing
├── src/
│   ├── models/                   # Data models
│   │   ├── user_profile.dart
│   │   ├── crop_image.dart
│   │   └── insurance_claim.dart
│   ├── services/                 # Firebase services
│   │   ├── firebase_auth_service.dart
│   │   └── firestore_service.dart
│   └── features/                 # App features
│       ├── auth/presentation/login_screen.dart
│       ├── dashboard/presentation/dashboard_screen.dart
│       ├── crop_monitoring/capture_image_screen.dart
│       ├── claims/file_claim_screen.dart
│       └── schemes/schemes_screen.dart
```

## 🔑 Key Files to Configure

1. **Firebase Config**: 
   - `android/app/google-services.json` (download from Firebase)
   - `lib/firebase_options.dart` (auto-generated by flutterfire)

2. **App Identity**:
   - `android/app/src/main/AndroidManifest.xml` (app name, permissions)
   - `android/app/build.gradle.kts` (package name, versions)

3. **Dependencies**:
   - `pubspec.yaml` (all packages)

## 📞 Support

- **Documentation**: See `SETUP_GUIDE.md`, `PROJECT_SUMMARY.md`
- **Firebase**: See `FIREBASE_SETUP.md`
- **Issues**: Check error messages in terminal
- **Help**: Run `flutter doctor -v` for detailed diagnostics

## ✅ Pre-Flight Checklist

Before running:
- [ ] Flutter installed (`flutter --version`)
- [ ] Android device/emulator connected (`flutter devices`)
- [ ] Dependencies installed (`flutter pub get`)
- [ ] Firebase configured (if using backend features)
- [ ] Permissions granted on device

## 🎯 Common Tasks

| Task | Command |
|------|---------|
| Fresh start | `flutter clean && flutter pub get && flutter run` |
| Release build | `flutter build apk --release` |
| Check errors | `flutter analyze` |
| Update packages | `flutter pub upgrade` |
| View app size | `flutter build apk --analyze-size` |
| Format code | `dart format .` |

---

**Quick Help**: `flutter --help` or `flutter [command] --help`
